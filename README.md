The 4 images in the main branch are used for the linkedin, instagram, NWIC logo, and facebook images in the NWIC signature.
In the folder named Signature User Pictures, that is where the images for each user signature are kept, as well as the default image, named Default Image
